# IEUM 구현 작업 규칙

이 파일은 저장소 구현을 위한 지침이다. 제품 요구, 세부 설계, 실험 결과를 대신하지 않는다.

## 먼저 읽을 문서

- [README](README.md)
- [제품 목적](docs/product/vision-and-scope.md)
- [Core 구조](docs/architecture/judgement-core.md)
- [실험·구현 계획](docs/plan/core-lab-experiment-plan.md)

세부 계약의 기준 문서는 도메인, 점수, 구조·파생, 런타임 문서로 분리되어 있다. 문서 간 충돌을 발견하면 임의로 혼합하지 말고 더 구체적인 계약을 확인하고 수정 이유를 기록한다. `Superseded` ADR을 현재 규칙으로 적용하지 않는다.

## 기본 작업 범위

별도 지시가 없는 최초 구현은 계획서의 **M0 + M1**에 한정한다. 순수 TS 계산, 파일 어댑터, CLI, 합성 fixture, 시점 기반 Replay, 보류 가능한 연결 후보, 원본 참조가 있는 evidence pack을 만든다. DB·API·웹 UI·생성형 모델·구조 변경기를 선행 구현하지 않는다. M2의 임베딩 비교는 M1 측정 후 진행하며, lexical 정확도 목표 통과를 전제하지 않는다.

## 반드시 지킬 불변식

1. 원문을 정규화·요약 결과로 덮어쓰지 않는다. 출처는 원문 revision과 범위를 참조한다.
2. 하나의 ThoughtUnit에 여러 ContextMembership을 허용한다. primary는 선택 사항이며 최대 하나다.
3. 검색 유사도·사용자 승인·동일 의견의 반복을 사실의 진실성으로 해석하지 않는다.
4. 점수는 확률이 아니다. 보정된 모델과 독립 평가가 없으면 확률 필드는 null이다.
5. `missing`, 측정된 0, 명시적 반박, 사용자 미응답을 구분한다.
6. 현재 입력·미래 데이터·평가 정답을 ContextProfile이나 검색 인덱스에 넣지 않는다.
7. 의미 변경은 사용자 명령 또는 승인된 Proposal에만 적용한다. 생성 모델은 쓰기 권한이 없다.
8. 판단은 원문·맥락·검색 결과·설정·모델의 revision을 기록한다. 같은 snapshot을 재사용한 반복 실행은 새로운 증거가 아니다.
9. 명시적 정답 지정은 추천 성능에서 분리한다. 추천을 보지 않은 사례를 거절로 라벨링하지 않는다.
10. 실제 개인정보·비공개 데이터·임베딩·로그·키를 공개 저장소에 넣지 않는다.

## 구현 기준

`packages/core`는 동기적 계산 함수와 도메인 타입 중심으로 유지한다. DB·네트워크·모델 호출은 애플리케이션 계층/어댑터가 끝내고 불변 snapshot을 넘긴다. evaluator마다 비동기 쿼리를 실행하는 구조를 만들지 않는다.

strict TypeScript, finite number 검증, 안정적인 동점 처리, 주입된 clock, 명시적 seed, 빈 집합 처리, typed failure를 사용한다. 틀린 정답 하나를 맞추기 위한 case ID 하드코딩을 금지한다. 테스트용 명시 규칙은 production scoring과 분리한다.

## 변경 완료 보고

현재 단계에서 실제 존재하는 검사만 실행한다. 구현하지 않은 API·DB·UI의 테스트가 통과했다고 쓰지 않는다. 변경 파일, 실행 명령, 성공/실패, fixture와 실제 데이터의 구분, 실패 사례, 아직 측정하지 않은 성능을 보고한다. 의존성 lockfile과 데이터/설정 hash를 남긴다.

가중치·임계값·모델·정규화기 변경은 비교 결과와 함께 제출한다. 최종 holdout을 보고 다시 조정하면 그 holdout은 개발 데이터가 된 것으로 표시하고 새로운 평가 구간을 확보한다.
