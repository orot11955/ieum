{
  "name": "@ieum/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "self-check": "node dist/main.js"
  },
  "dependencies": {
    "@ieum/backend": "workspace:*",
    "@nestjs/common": "12.0.3",
    "@nestjs/core": "12.0.3",
    "reflect-metadata": "0.2.2",
    "rxjs": "7.8.2"
  }
}
