# 실행 증거

기능 ID별 `<id 소문자>.md`에 실제 기준 SHA, 환경, 실행 명령/exit code, 정상·반례 결과, 산출물, 미검증 범위와 다음 조건을 남긴다. 계획이나 테스트 소스의 존재를 실행 성공으로 취급하지 않는다. private 원문·token·prompt·DB 연결문자열을 기록하지 않는다.

현재 증거: [BASE-01 저장소 준비](base-01.md). 이외 74개 카드의 기능 검증은 아직 없다.
